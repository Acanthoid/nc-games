import { Link } from "react-router-dom";

const NavBar = () => {
    return (
        <nav className="navBar">
            <Link to='/'>Home</Link>
            
        </nav>
    );
};
    
export default NavBar;